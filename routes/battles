const express = require('express');
const router = express.Router();

// Example route for battles
router.get('/', (req, res) => {
  res.send('Welcome to the Dance Battles!');
});

module.exports = router;
